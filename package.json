{
  "name": "dsh-web-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Base-path adapter so the official dsh web UI works behind code-server /proxy/<port>/ port forwarding.",
  "type": "module",
  "bin": {
    "dsh-web-proxy": "proxy.js"
  },
  "engines": {
    "node": ">=18"
  }
}
